# 决策日志：v1 的决策记录与术语表怎么补

## 2026-09-20 22:02 · 第 1 次 grill

起因是「补一下对应的记录」——v1 已经实现完、git 里也有 3 个提交了，但只有 `DESIGN.md` 一份大而全的设计文档，
决策散落在会话里。开问前先摸了仓库，查到一条关键事实：`DESIGN.md` 第 8.2 节写的 JSON 导入契约
（`{"questions":[...]}` 包装对象）与 `QuestionImportExportController` 的实际实现（裸数组）**不一致**，
而 9-18 建的 `md-to-question-bank` skill 已经按裸数组在出文件了。这条矛盾成了本轮第一个问题。

本轮同时跑了 domain-modeling，术语表 `CONTEXT.md` 与 `docs/adr/0001~0008` 在同一会话内落地。

### D1 · JSON 导入契约以裸数组为准，回头改文档

- **决策**：`POST /api/questions/import/json` 收裸数组 `[QuestionImportDTO]`，把 `DESIGN.md` 8.2 节改成裸数组，不动实现。
- **因为**：采纳推荐（A 选项）。
- **放弃**：B「改实现成包装对象」——已生成的题库文件全部失效；C「两个都收」——为一个内部工具维护两套解析路径，不值。
- **来源**：Q1
- 相关：[ADR 0006](../adr/0006-bare-array-import-contract.md)（含「将来要加元信息时必须改成包装对象」的后果说明）

> **Q1 - JSON 导入契约以哪个为准**：`DESIGN.md:359-377` 写的是 `{"questions":[...]}` 包装对象，但 `QuestionImportExportController.importJson` 收的是**裸数组** `[QuestionImportDTO]`。而 9-18 建的 `md-to-question-bank` skill 已经按裸数组出文件了——这个分歧不解决，以后每次生成题库都会踩。
>
> A. 以实现为准取**裸数组**，回头把 DESIGN.md 那段改掉
> B. 改实现成包装对象（会让已生成的题库文件全部失效）
> C. 两个都收，加兼容层
>
> ➡️ 推荐：A。裸数组的好处是"文件即一屏题目"，diff 干净、粘贴进导入框不用剥壳。C 看着两全其美，但等于给一个内部工具维护两套解析路径，不值。
>
> **你的回答**：按你推荐的来（= A）

### D2 · 「一次作答」的概念名定为「会话」

- **决策**：代码、文档、术语表统一用**会话**（Session）；「答题记录」降级为列表页的页面标题，不作为概念名。
- **因为**：采纳推荐（A 选项）。
- **放弃**：B「答卷」——自带考试语义，与 ADR-0001 定的练习定位冲突；C「一次练习」——像动词短语，做不了实体名。
- **来源**：Q2
- 相关：已落进 [CONTEXT.md](../../CONTEXT.md)「一次作答」组

> **Q2 -「一次作答」叫什么**：`quiz_session` 在代码里是 session，UI 列表页标题叫"答题记录"，我在文档里混用过"会话""答卷""一次练习"。
>
> A. **会话**（Session）——代码、文档、术语表统一用这个词；"答题记录"只是列表页的页面标题，不作为术语
> B. 答卷
> C. 一次练习
>
> ➡️ 推荐：A。"答卷"自带考试语义，跟 1A 定的练习定位冲突；"一次练习"读起来像动词短语，做不了实体名。
>
> **你的回答**：按你推荐的来（= A）

### D3 · 错题本定性为「投影出来的状态」，不建实体

- **决策**：错题本不是独立实体、没有对应的表，术语表明确写成「从作答统计里投影出来的状态」。
- **因为**：采纳推荐（A 选项）。
- **放弃**：B「独立成实体、复制题目快照」——引入数据冗余，还会带出"题目改了错题本里要不要跟着改"的同步问题；而 v1 的需求（看列表、重练、移出）用投影完全够。
- **来源**：Q3
- 相关：已落进 [CONTEXT.md](../../CONTEXT.md)「掌握与错题」组

> **Q3 -「错题本」是不是一个实体**：实现上它**没有对应的表**，是 `question_stat.in_wrong_book` 的投影。术语表要怎么写它？
>
> A. 错题本是**由作答统计投影出的状态**，不是实体——术语表明确写"无对应表"，避免后来人去找 `wrong_book` 表
> B. 独立成实体（把题目快照复制一份进去），方便以后做错题集、按本导出
>
> ➡️ 推荐：A。B 会引入数据冗余和"题目改了错题本里要不要跟着改"的一堆同步问题，而 v1 的错题本需求（看列表、重练、移出）用投影完全够。
>
> **你的回答**：按你推荐的来（= A）

### D4 · 归属命名用「公开题 / 我的题目」，UI 筛选文案不动

- **决策**：术语统一为**公开题**（不属于任何人、所有人可读不可改）与**我的题目**（用户自建、只有本人可改）；前端筛选项「全部 / 我的题库 / 公开题库」原样保留。
- **因为**：采纳推荐（A 选项）。UI 上的"题库"是筛选维度的口语说法，改它要动前端文案；而术语表只需要一个精确说法。
- **放弃**：B「统一叫公开题库 / 私有题库，把『题』全换成『题库』」——要动前端文案，且与「试卷」更容易混。
- **来源**：Q4
- 后果：同时确认了一条事实并写进记录——**用户自建的题目一律是"我的题目"，页面没有"设为公开"的入口**，公开题目前只由种子数据产生。
- 相关：已落进 [CONTEXT.md](../../CONTEXT.md)「归属」组

> **Q4 - 题目归属的叫法**：`owner_id IS NULL` 的表语义是"公开题"，但 UI 筛选项写的是"公开题库 / 我的题库"，容易和"试卷"混。
>
> A. 术语统一为**公开题**（owner_id 为空，所有人只读）与**我的题目**（owner_id = 我）；UI 筛选项保留"全部 / 我的题库 / 公开题库"不变
> B. 统一叫"公开题库 / 私有题库"，把"题"全换成"题库"
>
> ➡️ 推荐：A。UI 上的"题库"是筛选维度的口语说法，改它要动前端文案；而术语表只需要一个精确说法。注意这里有个事实要写进记录：**用户自建的题目一律是"我的题目"，页面没有"设为公开"的入口**，公开题目前只由种子数据产生。
>
> **你的回答**：按你推荐的来（= A）

### D5 · ADR 只收「够格」的 8 条，不搞全量

- **决策**：按「难反转 / 不看上下文会意外 / 有真实取舍」三条标准筛，只写 8 条；其余 12+ 个决策继续留在 `DESIGN.md`。
- **因为**：采纳推荐（A 选项）。ADR 的价值密度在于"少而重"，换起来只改几个 import 的事写成 ADR 只会稀释真正重要的那几条。
- **放弃**：B「20+ 条全量写成 ADR」；C「精简到 4 条」。
- **来源**：Q5
- 相关：[docs/adr/](../adr/)（0001~0008）

> **Q5 - ADR 收录范围**：20 个决策点里，按「难反转 / 不看上下文会意外 / 有真实取舍」三条标准，我认为够格的只有 8 条：① 练习语义优先于考试语义 ② 逐题即时判分 + 进度落库不引 Redis ③ owner_id 为空表示公开题 ④ 多选全对才得分 ⑤ 导入跳过错误行 ⑥ JSON 裸数组契约 ⑦ dev/prod 双 compose + MySQL 绑定挂载 ⑧ 正确答案存 `question.answer` 而非 `option.is_correct`。
>
> A. 只写这 8 条，其余决策继续留在 DESIGN.md
> B. 20+ 条全量写成 ADR
> C. 精简到 4 条最硬的
>
> ➡️ 推荐：A。ADR 的价值密度在于"少而重"——比如"用 Element Plus 而不是 Ant Design Vue"这种换起来就是改几个 import 的事，写成 ADR 只会稀释真正重要的那几条。
>
> **你的回答**：按你推荐的来（= A）

### 未决问题

- 要不要开放「把自己建的题设为公开」——卡在：得先回答"别人正在练的公开题被作者删掉"怎么办。这是 Q4 暴露出来的，当时按收窄处理（不开放）记进了 ADR-0003，但没讨论过开放后的方案。
- 「试卷 / 固定卷 / 规则卷」这组叫法——卡在：**这轮根本没问**，是我写术语表时直接定的。要不要补一轮确认，或者就这么默认接受。
- 数据备份策略与镜像 tag / 分发方式——卡在：未拍板（healthcheck 那项已由 ADR 0012 收掉，这两项仍开着）。
- 术语表与代码脱钩的风险——卡在：没有机制保证改代码时同步改 `CONTEXT.md`，目前只靠人记得。

### 本次定下的叫法

这些在同一会话内已由 domain-modeling 落进 [CONTEXT.md](../../CONTEXT.md)，此处只做罗列备查：

- **会话** = 用户某一次完整的作答过程（含题目、作答与判分结果）。不是"答卷"；"答题记录"只是列表页标题。
- **作答** = 用户对一道题提交的选择，可留空表示跳过。**答案**专指正确答案，两个词不再混用。
- **公开题** = 不属于任何人的题目，所有人可读、谁都不可改。
- **我的题目** = 用户自建、只有本人可改的题目。
- **错题本** = 从作答统计投影出来的状态集合，不是实体、无对应表。
- **题目契约** = 批量导入时一道题必须满足的字段约定。
- **错误行** = 导入时校验不通过、被单独跳过的题目。
- 另明确：**本项目没有「知识点」概念**，一道题考什么靠分类 + 标签组合表达。
