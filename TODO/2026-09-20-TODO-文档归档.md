# 待办：grill 问答原文要不要进仓库

创建：2026-09-20 · 状态：待办 · 优先级：低

`docs/adr/` 里存的是决策**结论**（选了什么、为什么）。而「当时问了什么、有哪些候选选项、我推荐哪个」那份完整问答，在 `.workbuddy/decisions/2026-09-20-docker-run.md`，被 `.gitignore` 挡着，不跟仓库走。

要不要挪进来（比如 `docs/decisions/`），取决于你觉得它值不值得留给以后的自己——ADR 只留结论是故意的，过程记在别处也说得通。

倾向于先不动：`.workbuddy/` 是会话记忆，仓库只留结论，职责清楚。等哪次真的回头想看「当初为什么排除掉另一个方案」，再搬也不迟。
